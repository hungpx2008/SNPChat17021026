╔════════════════════════════════════════════════════════════════════════════════╗
║                   CHATSNP LLM CONFIGURATION QUICK REFERENCE                    ║
╚════════════════════════════════════════════════════════════════════════════════╝

📍 CONFIGURATION FILES BY LAYER
═══════════════════════════════════════════════════════════════════════════════════

┌─ ORCHESTRATION (Docker Compose) ─────────────────────────────────────────────────
│
│  docker-compose.yml (Development)
│  ├─ LLM_MODEL:            ${LLM_MODEL:-openai/gpt-4o-mini}
│  ├─ OPENAI_API_KEY:       ${OPENAI_API_KEY:-}
│  ├─ OPENAI_BASE_URL:      ${OPENAI_BASE_URL:-https://openrouter.ai/api/v1}
│  ├─ OPENROUTER_API_KEY:   ${OPENROUTER_API_KEY:-}
│  ├─ OPENROUTER_API_BASE:  ${OPENROUTER_API_BASE:-https://openrouter.ai/api/v1}
│  └─ LOCAL_LLM_MODEL:      ${LOCAL_LLM_MODEL:-openai/gpt-4o-mini}
│
│  docker-compose.prod.yml (Production with Mem0 Service)
│  ├─ [mem0] LLM_MODEL:     ${LLM_MODEL:-openai/gpt-4o-mini}
│  └─ [frontend] LOCAL_LLM_MODEL: ${LOCAL_LLM_MODEL:-openai/gpt-5-nano}
│
│  docker-compose.pro.yml (Production without Mem0)
│  └─ [frontend] LOCAL_LLM_MODEL: ${LOCAL_LLM_MODEL:-openai/gpt-4o-mini}
│
└─────────────────────────────────────────────────────────────────────────────────

┌─ ENVIRONMENT VARIABLES ──────────────────────────────────────────────────────────
│
│  .env (ACTIVE - Development)
│  ├─ OPENAI_API_KEY=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
│  ├─ OPENAI_BASE_URL=https://openrouter.ai/api/v1
│  ├─ OPENROUTER_API_KEY=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
│  ├─ OPENROUTER_API_BASE=https://openrouter.ai/api/v1
│  ├─ LLM_MODEL=openai/gpt-4o-mini
│  └─ LOCAL_LLM_MODEL=openai/gpt-4o-mini
│
│  .env.example (Template)
│  ├─ LLM_PROVIDER=openai
│  ├─ LLM_MODEL=openai/gpt-5-nano ⚠️  MISMATCH with .env
│  └─ NEXT_PUBLIC_LOCAL_LLM_MODEL=openai/gpt-5-nano
│
│  .env.production (Secrets from CI/CD)
│  └─ NEXT_PUBLIC_LOCAL_LLM_MODEL=openai/gpt-5-nano
│
│  frontend/.env.local (Frontend Dev)
│  ├─ LOCAL_LLM_MODEL=openai/gpt-4o-mini
│  └─ NEXT_PUBLIC_LOCAL_LLM_MODEL=openai/gpt-4o-mini
│
│  frontend/.env.local.example (Local Ollama Template)
│  ├─ OPENAI_BASE_URL=http://127.0.0.1:1234/v1
│  └─ LOCAL_LLM_MODEL=qwen3-vl-4b-instruct-mlx
│
│  mem0-service/.env.example
│  ├─ LLM_PROVIDER=openai
│  └─ LLM_MODEL=openai/gpt-5-nano ⚠️  MISMATCH with docker-compose.prod.yml
│
└─────────────────────────────────────────────────────────────────────────────────

┌─ SOURCE CODE DEFAULTS ───────────────────────────────────────────────────────────
│
│  backend/src/core/config.py (Pydantic Settings)
│  ├─ openai_base_url: "https://openrouter.ai/api/v1" ✅
│  ├─ llm_model: "openai/gpt-5-nano" ⚠️  MISMATCH
│  ├─ openai_api_key: None
│  └─ openrouter_api_key: None
│
│  backend/src/core/mem0_local.py
│  ├─ LLM_PROVIDER: "openai"
│  ├─ LLM_MODEL: "openai/gpt-4o-mini" ✅
│  └─ API keys: None (falls back to env vars)
│
│  backend/src/worker/data_tasks.py
│  ├─ llm_model: "openai/gpt-5-nano" ⚠️  MISMATCH
│  ├─ openai_base: "https://openrouter.ai/api/v1" ✅
│  └─ openai_key: ""
│
│  mem0-service/main.py
│  ├─ LLM_PROVIDER: "openai"
│  └─ LLM_MODEL: "openai/gpt-5-nano" ⚠️  MISMATCH
│
│  frontend/src/ai/localClient.ts
│  ├─ LOCAL_LLM_BASE_URL: "https://openrouter.ai/api/v1" ✅ (fallback)
│  ├─ LOCAL_LLM_MODEL: "gpt-oss-120b" ⚠️  HARDCODED FALLBACK
│  └─ apiKey: fallback chain (OPENROUTER → OPENAI → "not-needed")
│
└─────────────────────────────────────────────────────────────────────────────────


🚨 CRITICAL MISMATCHES DETECTED
═══════════════════════════════════════════════════════════════════════════════════

1. LLM_MODEL DEFAULTS:
   ┌─────────────────────────────┬─────────────────────────────┐
   │ Component                   │ Default Value               │
   ├─────────────────────────────┼─────────────────────────────┤
   │ .env (ACTIVE)               │ openai/gpt-4o-mini          │
   │ .env.example                │ openai/gpt-5-nano    ❌     │
   │ docker-compose.yml          │ openai/gpt-4o-mini          │
   │ docker-compose.prod.yml     │ openai/gpt-4o-mini          │
   │ backend/src/core/config.py  │ openai/gpt-5-nano    ❌     │
   │ backend/src/core/mem0_local │ openai/gpt-4o-mini          │
   │ backend/src/worker/data     │ openai/gpt-5-nano    ❌     │
   │ mem0-service/main.py        │ openai/gpt-5-nano    ❌     │
   │ frontend/localClient.ts     │ gpt-oss-120b (fallback) ❌  │
   └─────────────────────────────┴─────────────────────────────┘

2. FRONTEND BASE URL FALLBACK CHAIN:
   process.env.OPENAI_BASE_URL
        ↓
   process.env.OPENROUTER_API_BASE
        ↓
   'https://openrouter.ai/api/v1' ✅ (eventually safe)

3. FRONTEND MODEL FALLBACK CHAIN:
   process.env.LOCAL_LLM_MODEL
        ↓
   process.env.NEXT_PUBLIC_LOCAL_LLM_MODEL
        ↓
   'gpt-oss-120b' ❌ (might not work!)


💡 RECOMMENDED FIXES
═══════════════════════════════════════════════════════════════════════════════════

1. Standardize LLM_MODEL to ONE value across all files:
   
   OPTION A: Use openai/gpt-4o-mini everywhere (faster, cheaper)
   - Update: .env.example (line 58) → gpt-4o-mini
   - Update: backend/src/core/config.py (line 24) → gpt-4o-mini
   - Update: backend/src/worker/data_tasks.py (line 24) → gpt-4o-mini
   - Update: mem0-service/main.py (line 25) → gpt-4o-mini
   - Update: mem0-service/.env.example (line 8) → gpt-4o-mini
   
   OPTION B: Use openai/gpt-5-nano everywhere (placeholder name)
   - Update: .env (line 16) → gpt-5-nano
   - Update: docker-compose.yml (line 22) → gpt-5-nano
   - Update: docker-compose.prod.yml (line 59) → gpt-5-nano

2. Fix frontend fallback chain:
   - Update: frontend/src/ai/localClient.ts (line 11)
   - From: 'gpt-oss-120b'
   - To: 'openai/gpt-4o-mini' (or chosen standard)

3. Ensure .env.production doesn't need keys (use GitHub Secrets):
   - Current: GOOD - no keys in .env.production
   - Add documentation for CI/CD setup


📊 CONFIGURATION FLOW
═══════════════════════════════════════════════════════════════════════════════════

ENV FILES (.env / .env.local)
         ↓
   DOCKER COMPOSE (reads .env via interpolation)
         ↓
   CONTAINERS (environment section sets vars)
         ├─→ BACKEND: backend/src/core/config.py reads Settings
         ├─→ WORKERS: backend/src/worker/{chat,data}.py use os.getenv()
         ├─→ MEM0: mem0-service/main.py uses os.environ.get()
         └─→ FRONTEND: next.config.js bakes vars at build time
                       frontend/src/ai/localClient.ts reads process.env


🔐 SECRETS MANAGEMENT
═══════════════════════════════════════════════════════════════════════════════════

✅ Current Setup (Good):
   - .env (dev): Contains working keys for testing
   - .env.production: NO secrets (use GitHub Secrets/CI)
   - frontend/.env.local: Isolated from repo for local dev
   - mem0-service/.env.example: Placeholders only

⚠️  To Improve:
   - Add .env to .gitignore (if not already) ← CRITICAL!
   - Use GitHub Secrets for CI/CD deployments
   - Document env var setup in README.md


📋 FILES TO UPDATE FOR EDITS
═══════════════════════════════════════════════════════════════════════════════════

PRIMARY (Always):
   1. chatSNP170226/.env

SECONDARY (If changing defaults):
   2. docker-compose.yml
   3. docker-compose.prod.yml
   4. docker-compose.pro.yml

TERTIARY (If changing code logic):
   5. backend/src/core/config.py
   6. backend/src/core/mem0_local.py
   7. backend/src/worker/data_tasks.py
   8. backend/src/worker/chat_tasks.py (if needed)
   9. mem0-service/main.py
  10. frontend/src/ai/localClient.ts

DOCUMENTATION:
  11. .env.example
  12. .env.production (reference only)
  13. mem0-service/.env.example
  14. frontend/.env.local.example


═══════════════════════════════════════════════════════════════════════════════════

For detailed line-by-line reference, see: LLM_CONFIG_INVENTORY.md

